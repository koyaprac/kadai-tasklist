<h1>id: <%= @task.id %> のタスク詳細</h1>

<table class="table table-bordered">
    <tr>
      <th>id</th>
      <td> <%= @task.id %></td>
    </tr>
    
    <tr>
      <th> Status</th>
      <td> <%= @task.status %></td>
    </tr>
    
    <tr>
      <th> タスク内容</th>
      <td> <%= @task.content %></td>
    </tr>
</table>

<div class="col-sm-2">
<%= link_to "このタスクを編集する", edit_task_path(@task), class: "btn btn-default" %>
</div>
<div class="col-sm-offset-1 col-sm-1">
<%= link_to "このタスクを削除する", @message, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: "btn btn-danger" %></br>
</div>