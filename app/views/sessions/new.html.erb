<div class='text-center' >
  <h1>Log in</h1>
</div>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <%= form_for(:session, url: login_path) do |f| %>
      <%= render '/layouts/user_form', f: f %>
      <% end %>
    <p> New User ? <%= link_to 'Sign up now !', signup_path %></p>
  </div>
</div>